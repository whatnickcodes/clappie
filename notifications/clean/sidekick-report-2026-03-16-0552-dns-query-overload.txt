[sidekick:2026-03-16-0552-internal-investigate-dns-query-overload-on] DIAGNOSTIC REPORT: DNS Concurrent Query Overload — 100.101.18.71

**FINDINGS:**

1. **Frequency & Duration:**
   - 414 warnings in the last 60 minutes (04:52:33 to 05:52:31 UTC)
   - Warnings still actively occurring as of 05:52 UTC — this is ONGOING
   - Average: ~7 warnings per minute

2. **Root Cause — Client 100.101.18.71:**
   - Responsible for 432/437 queries (98.8%) in recent query log
   - 368/432 queries (85%) are for dhl.com with query type ANY
   - Queries are getting 'config error is REFUSED' response
   - Client is NOT backing off — immediately retrying same failed query
   - All 368 dhl.com queries happen within single seconds (e.g., Mar 16 05:53:14, 20+ queries in that one second)

3. **Configuration:**
   - dnsmasq max concurrent limit: 150 (default, not explicitly configured)
   - No custom maxConcurrent setting in pihole.toml
   - Limit is reasonable for normal traffic but INSUFFICIENT for this client's flood

4. **Container Status:**
   - Container restarted at 05:50:46 UTC
   - Client immediately resumed flood within 4 seconds (first warning at 05:50:50)
   - FTL log shows upstream DNS timeout at 05:52:53 (Unbound probably overloaded too)

5. **Other Clients:**
   - 192.168.1.186: 2 queries (Amazon domain queries)
   - 127.0.0.1: 2 queries (localhost)
   - 192.168.1.97: 1 query
   - All other traffic is negligible

**PATTERN ASSESSMENT:**
This is a classic retry loop. Client 100.101.18.71 is attempting 'ANY' record queries for dhl.com, receiving REFUSED, and immediately retrying without backoff. This floods the 150-concurrent-query window and causes legitimate DNS requests to be dropped.

**Root Cause NOT the 150-query limit itself** — the limit is appropriate. The root cause is: **misbehaving client application on 100.101.18.71 stuck in infinite retry loop, or compromised/misconfigured to deliberately spam dhl.com queries.**

**Recommendation for Marco:**
Check what's running on 100.101.18.71 (IP is on the Tailscale network). This client needs to be investigated or isolated.
