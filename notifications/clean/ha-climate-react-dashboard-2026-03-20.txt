Climate React dashboard icons are blank — automation skips the indicator booleans.

A sidekick investigated why the living room CR card shows grey/inactive icons even when heating is active. Root cause: automation.climate_react_year_round reads input_select.living_room_climate_mode correctly but never toggles the input_booleans the dashboard card watches (heating, cooling, fan — all stuck OFF). It goes straight to climate.set_temperature.

Recommended fix: rewire dashboard card to derive icon state from input_select.living_room_climate_mode directly (single source of truth). Same pattern likely affects stanza_andrea / AC Andy.

Washnote pushed to homeops. Needs a laptop session to edit HA dashboard YAML.

---
[meta]
source: sidekick-report
source_id: sidekick-2026-03-18-0450-telegram-bot-couldnt-intervene-before-is-the
created: 2026-03-20 06:58
icon: 🏠
title: Climate React dashboard icons broken — needs HA YAML fix
summary: CR card icons always grey. Automation doesn't update indicator booleans. Fix: rewire card to read input_select directly. Needs laptop session.
