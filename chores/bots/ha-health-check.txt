Check Home Assistant API reachability via Tailscale.

Steps:
1. Run:
   ```
   curl -sf -o /dev/null -w "%{http_code}" \
     -H "Authorization: Bearer $HA_TOKEN" \
     http://100.116.172.30:8123/api/
   ```
   Expected: 200
2. On non-200:
   - Log the failure
   - Check previous heartbeat log entries for consecutive failures
   - If 3+ consecutive non-200 results → escalate:
     `clappie sidekick spawn "telegram user YOUR_ID: HA API unreachable for 3+ checks (status: [code]). Tailscale path may be down. Manual investigation required."`
   - Single failure → log, retry next cycle
3. On 200 → silent, no noise for healthy state

Logging: append one line to recall/logs/heartbeat/YYYY-MM-DD.txt
Format: [HH:MM] ha-health-check ✓ 200 (or: ✗ [status_code] — consecutive: N)
NEVER create standalone files. ALWAYS append ONE line to the daily file.

For detailed remediation procedures, see:
- CLAUDE.md Routing → homeops/docs/domains/home-assistant/ha-operations.md
- homeops trigger-index → "HA automation behavior unexpectedly changed"
- homeops/docs/domains/access/shared-access.md for endpoint details

[heartbeat-meta]
interval: 1h
model: haiku
last_run: 2026-03-17T17:25
